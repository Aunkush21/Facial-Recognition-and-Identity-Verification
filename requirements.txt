# --- Web framework ---
fastapi==0.137.1
uvicorn[standard]==0.34.0
python-multipart==0.0.20
jinja2==3.1.5

# --- Config / env ---
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Database ---
sqlalchemy==2.0.39
alembic==1.14.1
psycopg2-binary==2.9.10
pgvector==0.3.6

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
# passlib 1.7.4 (final release) reads bcrypt.__about__.__version__, removed in
# bcrypt >= 4.1 — pin to the last compatible release to avoid the version-read error.
bcrypt==4.0.1

# --- Face engine (Phase 2) ---
opencv-python==4.13.0.92
numpy==2.1.3
insightface==1.0.1
onnxruntime==1.27.0
scikit-image==0.25.0
huggingface_hub==1.20.1

# --- Data / utils ---
pandas==2.2.3
openpyxl==3.1.5

# --- Testing (Phase 5) ---
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
pytest-mock==3.14.0
ruff==0.8.6
